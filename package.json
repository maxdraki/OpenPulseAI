{
  "name": "openpulseai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:sea:mcp": "bash scripts/build-sea.sh mcp-server",
    "build:sea:dream": "bash scripts/build-sea.sh dream",
    "build:sea:skills": "npx esbuild packages/core/dist/skills/cli.js --bundle --platform=node --target=node20 --format=cjs --outfile=dist/skills.cjs && echo 'Skills CLI bundled to dist/skills.cjs — run: node dist/skills.cjs'",
    "dev:desktop": "cargo tauri dev",
    "build:desktop": "bash scripts/build-desktop.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "esbuild": "^0.25.0"
  }
}
